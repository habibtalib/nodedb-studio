---
phase: 02-connect-auth-capabilities
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - nodedb-studio/src/modals/new_connection.rs
  - nodedb-studio/src/views/connection_manager.rs
  - nodedb-studio/src/app.rs
  - nodedb-studio/src/components/topbar.rs
  - nodedb-studio/src/components/popovers/connection_popover.rs
  - nodedb-studio/src/components/command_palette.rs
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, CONN-06, CONN-07]
nyquist_compliant: true

must_haves:
  truths:
    - "The connection form has an Auth method dropdown that swaps fields per mode (trust/password/api_key/oidc) and defaults the port to 6433"
    - "Clicking connect collects the in-session secret, runs connect_real at the seam, shows Connecting then an inline error+Retry on failure, and never enters the connected state on failure"
    - "On success the app transitions to the Studio shell bound to the live session with real capabilities and identity"
    - "The topbar chip shows the real user, role, current database, databases count, and server version from the live session"
    - "Disconnect (Cmd-D) releases the live client and returns to the connection manager"
    - "Quick-switch (palette + connection popover) reuses the real connect path"
  artifacts:
    - path: "nodedb-studio/src/modals/new_connection.rs"
      provides: "Auth-method dropdown with field-swap, port 6433, in-session secret copy (D-02)"
      contains: "6433"
    - path: "nodedb-studio/src/views/connection_manager.rs"
      provides: "Inline connecting/error/Retry card driving connect_real (D-03, CONN-03)"
      contains: "connect_real"
    - path: "nodedb-studio/src/app.rs"
      provides: "Provides NodeDbConnectionService for the real connect path + disconnect release"
      contains: "NodeDbConnectionService"
  key_links:
    - from: "nodedb-studio/src/views/connection_manager.rs"
      to: "NodeDbConnectionService::connect_real"
      via: "spawn at the seam; active.set(Some) only on Ok"
      pattern: "connect_real"
    - from: "nodedb-studio/src/components/topbar.rs"
      to: "ActiveConnection (live session)"
      via: "renders user/role/current_database/sub/version chip"
      pattern: "c.user|c.role|current_database"
    - from: "nodedb-studio/src/app.rs"
      to: "NodeDbConnectionService::disconnect"
      via: "Cmd-D / disconnect handler releases the client"
      pattern: "disconnect"
---

<objective>
Wire the real connect path into the UI: rework the connection form (D-02 auth-method dropdown +
field-swap + port 6433), drive the inline Connecting/error/Retry card off `connect_real` (D-03,
CONN-03), transition into the `Studio` shell on success (CONN-04), render the real identity chip
(CONN-06), release the client on disconnect (CONN-07), and reuse the real connect path for
quick-switch (D-08). A human-verify checkpoint confirms the end-to-end flow against a live `:6433`.

Purpose: This is where the user actually opens a real session. The seam (Plan 02) and data
contracts (Plan 01) are in place; this plan connects them to buttons and the shell.

Output: Reworked form + connection manager + app wiring + topbar chip + quick-switch, plus a
manual end-to-end verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-connect-auth-capabilities/02-CONTEXT.md
@.planning/phases/02-connect-auth-capabilities/02-RESEARCH.md
@.planning/phases/02-connect-auth-capabilities/02-VALIDATION.md

<interfaces>
<!-- From Plans 02-01 / 02-02 (already landed) + existing code. -->

Seam (Plan 02) — call from the UI for the REAL connect:
```rust
// NodeDbConnectionService (nodedb-studio/src/services/nodedb_service.rs)
pub async fn connect_real(&self, saved: &SavedConnection, secret: AuthInput) -> Result<ActiveConnection, StudioError>;
pub fn disconnect(&self);
// auth.rs
pub struct Secret; impl Secret { pub fn new(s: impl Into<String>) -> Self; }
pub enum AuthInput { Trust{username}, Password{username, password: Secret}, ApiKey{token: Secret}, Oidc{token: Secret, provider: Option<String>} }
```

SavedConnection (Plan 01) — connect-config, no secret:
```rust
pub struct SavedConnection { name, meta, sub, status, db_count, ping, server,
    host: String, port: u16, auth_mode: AuthMode, username: Option<String>,
    default_database: Option<String>, tls: TlsSettings, connect_timeout_secs: Option<u64> }
pub enum AuthMode { Trust, Password, ApiKey, OidcBearer }
```

Reusable UI primitive (Phase 1):
- AsyncView component (nodedb-studio/src/components/async_view.rs) props: loading, empty, error: Option<String>, retriable, on_retry, empty_message.
- StudioError::is_retriable() gates Retry (Connection→retriable, Auth→not).

Existing connect handler shape (connection_manager.rs:50-63) — currently swallows Err AND passes only the name:
```rust
// ConnectionCard prop today (connection_manager.rs:85):
fn ConnectionCard(conn: SavedConnection, on_connect: EventHandler<String>) -> Element
// handler today (connection_manager.rs:103-107): on_connect.call(name.clone()) — name only.
// manager handler today: spawn(async move { if let Ok(session) = service.connect(&name).await { active.set(Some(session)); } });
```

ActiveConnection fields used by topbar (connection.rs): name, sub, user, role, current_database, capabilities, databases.
Topbar already renders c.name, c.sub, c.current_database, c.avatar_letter(), c.capabilities.readonly (topbar.rs).
Topbar does NOT yet render c.user or c.role — those are net-new chip elements in Task 3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the new-connection form — auth-method dropdown, field-swap, port 6433 (D-02)</name>
  <files>nodedb-studio/src/modals/new_connection.rs</files>
  <read_first>
    - nodedb-studio/src/modals/new_connection.rs (current static form: port 2480, "Username+password/Token/mTLS" picker, "OS keychain" copy)
    - nodedb-studio/src/state/connections_registry.rs (AuthMode enum from Plan 01)
    - nodedb-studio/src/state/ui.rs (ModalKind — for the existing close handler)
    - .planning/phases/02-connect-auth-capabilities/02-CONTEXT.md § D-02 (field-swap matrix), § Deferred (mTLS demoted)
    - .planning/phases/02-connect-auth-capabilities/02-RESEARCH.md § Target 7 (port 6433), § State of the Art (form rework)
  </read_first>
  <action>
    Rework `NewConnectionForm` per D-02. Replace the stale picker and fix the port.

    1. Port: change the Port input default from `"2480"` to `"6433"`.
    2. Replace the auth `select` ("Username + password" / "Token" / "mTLS") with an Auth method
       dropdown whose options map to `AuthMode`: trust / password / api_key / oidc_bearer.
       (mTLS is NOT an option — it is a TLS transport toggle, not an auth mode, per Deferred.)
    3. Hold the selected auth mode in a `use_signal(|| AuthMode::Password)` and SWAP the visible
       field set per D-02:
       - Trust → Username only
       - Password → Username + Password
       - ApiKey → Token only
       - OidcBearer → Token + optional Provider hint
       Use a `match auth_mode.read().clone()` (or `*auth_mode.read()`) with an explicit arm per
       AuthMode variant — NO `_ =>` (AuthMode is the studio's own exhaustive enum). Render the
       relevant `input`s conditionally based on the matched variant.
    4. Replace the "Credentials are stored in your OS keychain." copy with in-session copy, e.g.
       "Secrets are kept in memory for this session only — re-entered on reconnect." (D-01).
    5. Keep the `onsubmit` form-submit discipline: if the form uses a `form { onsubmit: ... }`,
       call `e.prevent_default()` (Dioxus 0.7 forms submit by default). If it stays button-driven
       as today, no change needed — but do not introduce a submitting form without prevent_default.
    6. This task is form STRUCTURE only (fields + dropdown + copy). Persisting a new SavedConnection
       and triggering connect from "Save & connect" can stay wired to the existing modal-close
       behavior for now; the live connect entry point is the connection-manager card (Task 2). Keep
       the Cancel/close handler intact.
  </action>
  <verify>
    <automated>cargo build -p nodedb-studio && cargo clippy -p nodedb-studio --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `nodedb-studio/src/modals/new_connection.rs` contains `6433` and does NOT contain `2480` (verify: `grep -c 2480 nodedb-studio/src/modals/new_connection.rs` returns 0)
    - The form does NOT offer "mTLS" as an auth method and does NOT contain "OS keychain" copy (verify: `grep -niE 'mTLS|keychain' nodedb-studio/src/modals/new_connection.rs` returns nothing)
    - The auth-mode match has no `_ =>` arm (verify by reading the field-swap match)
    - `cargo build -p nodedb-studio` succeeds
  </acceptance_criteria>
  <done>
    The form defaults to port 6433, presents trust/password/api_key/oidc_bearer with the correct field-swap per mode, shows in-session-secret copy, and compiles clippy-clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Drive the inline connect card off connect_real, transition the shell, and release on disconnect (CONN-01/02/03/04/07)</name>
  <files>nodedb-studio/src/views/connection_manager.rs, nodedb-studio/src/app.rs, nodedb-studio/src/components/popovers/connection_popover.rs, nodedb-studio/src/components/command_palette.rs</files>
  <read_first>
    - nodedb-studio/src/views/connection_manager.rs (current ConnectionCard signature at line ~85: `on_connect: EventHandler<String>`, and the manager handler at lines 50-63 that swallows Err)
    - nodedb-studio/src/app.rs (service provision; the `_real` NodeDbConnectionService is built but unused; root active conditional)
    - nodedb-studio/src/services/nodedb_service.rs (connect_real + disconnect from Plan 02)
    - nodedb-studio/src/services/auth.rs (AuthInput + Secret from Plan 02)
    - nodedb-studio/src/components/async_view.rs (AsyncView props for the inline card)
    - nodedb-studio/src/services/error.rs (StudioError::is_retriable for the Retry gate)
    - nodedb-studio/src/components/popovers/connection_popover.rs and command_palette.rs (quick-switch currently calls service.connect by name)
    - nodedb-studio/src/routes.rs (Cmd-D handler sets active None — must also release the client)
    - .planning/phases/02-connect-auth-capabilities/02-RESEARCH.md § Architecture Patterns (client ownership; service swap vs interior-mutable; connect at the seam one-shot spawn), § Common Pitfalls 2/6, § Target 6 (failure→StudioError, Retry gate)
  </read_first>
  <action>
    Make the connection manager open a REAL session and the shell release it on disconnect. Per
    research architecture choice (B), `NodeDbConnectionService` owns the live client interior-mutably
    and `connect_real`/`disconnect` are its methods.

    1. app.rs: hold a single shared `Rc<NodeDbConnectionService>` reachable by the connect handlers
       AND the disconnect handler. Provide it via context (e.g. `use_context_provider(|| real.clone())`
       where `real: Rc<NodeDbConnectionService>`), in addition to the existing
       `Rc<dyn ConnectionService>` mock used for list_connections/notifications seeding. The mock
       stays the default seam for registry/notifications (it works alongside — seam discipline); the
       real service is the connect/disconnect engine. Remove the throwaway `_real` binding and the
       SEAM-02 "prove instantiable" comment now that it is genuinely used.

    2. connection_manager.rs — pass the FULL SavedConnection to the handler, collect the in-session
       secret, then drive the D-03 inline lifecycle.

       2a. CONNECTION-CARD PROP RESHAPE (BLOCKER fix — the handler needs the whole `SavedConnection`
           + an `AuthInput`, not just the name as today). Change the `ConnectionCard` prop from
           `on_connect: EventHandler<String>` to `on_connect: EventHandler<SavedConnection>`, and
           change the card's onclick body from `on_connect.call(name.clone())` to
           `on_connect.call(conn.clone())` (the card already owns `conn: SavedConnection`). The
           manager-level handler then receives the full `SavedConnection` directly — no
           registry-by-name lookup needed for the card path. (This is the chosen option of the two:
           prop-type change, NOT registry lookup.)

       2b. CONNECT STATE SIGNALS. In `ConnectionManager`, add two manager-scoped signals keyed by the
           connecting connection's name so the right card shows its state:
           `let mut connecting = use_signal(|| None::<String>);` (name of the card currently
           connecting, or None) and
           `let mut connect_error = use_signal(|| None::<(String /*name*/, String /*msg*/, bool /*retriable*/)>);`.
           Pass `connecting`/`connect_error` (Copy signals) down to each `ConnectionCard` (add props
           `connecting: Signal<Option<String>>`, `connect_error: Signal<Option<(String,String,bool)>>`)
           so the card can compare against its own `conn.name` to render Connecting…/error/Retry.

       2c. SECRET COLLECTION (WARNING fix — concrete inline mechanism; states exactly where `entered`
           comes from). Add a per-card inline secret `use_signal(String)` INSIDE `ConnectionCard`:
           `let mut secret_input = use_signal(String::new);`. Render this `input` (type "password" for
           Password/ApiKey/OidcBearer, type "text" allowed for the optional OIDC provider) ONLY when
           `conn.auth_mode` is Password/ApiKey/OidcBearer — Trust shows no secret input. The value
           typed into this input IS `entered`: read it as `secret_input.peek().clone()` inside the
           onclick handler (peek — we read+don't subscribe in the handler). This secret signal is
           card-local and ephemeral: it is moved into `Secret::new(...)` and never written to
           `SavedConnection` or persisted (D-01). For OidcBearer, optionally add a second card-local
           `use_signal(String)` for the provider hint (empty → `None`).
           Build the matching `AuthInput` from `conn.auth_mode` (entered = `secret_input.peek().clone()`):
           - AuthMode::Trust → AuthInput::Trust { username: conn.username.clone().unwrap_or_default() }
           - AuthMode::Password → AuthInput::Password { username: conn.username.clone().unwrap_or_default(), password: Secret::new(entered) }
           - AuthMode::ApiKey → AuthInput::ApiKey { token: Secret::new(entered) }
           - AuthMode::OidcBearer → AuthInput::Oidc { token: Secret::new(entered), provider: provider_nonempty }
           (match over AuthMode explicitly — no `_ =>`.) On the card's Connect onclick, build this
           `AuthInput`, then call `on_connect.call(conn.clone())` — but since the handler also needs
           the `AuthInput`, change the EventHandler payload to carry BOTH: make the prop
           `on_connect: EventHandler<(SavedConnection, AuthInput)>` and call
           `on_connect.call((conn.clone(), auth))`. (This supersedes 2a's bare `SavedConnection`
           payload: the final, exact signature is `EventHandler<(SavedConnection, AuthInput)>`.)
           D-08 note: because the secret is collected on the card itself for non-trust modes, the
           quick-switch path in step 4 — which has no card input — must route to the form/prompt when
           the target's secret isn't held; it must not connect silently with an empty secret.

       2d. RUN AT THE SEAM (one-shot `spawn`, NOT use_resource). In the manager-level
           `on_connect` handler (receives `(saved, auth)`):
           - set `connecting.set(Some(saved.name.clone()))`; clear `connect_error`.
           - clone the `Rc<NodeDbConnectionService>` (`real`) and `saved` into the async move.
           - after `real.connect_real(&saved, auth).await`:
             - Ok(session) → `active.set(Some(session))` (transitions to Studio — CONN-04);
               `connecting.set(None)`; `connect_error.set(None)`.
             - Err(e) → `connect_error.set(Some((saved.name.clone(), e.to_string(), e.is_retriable())))`;
               `connecting.set(None)`; DO NOT touch `active` (CONN-03 — never enters connected state
               on failure). Never hold a `.read()`/`.write()` guard across the `.await` (set signals
               only after await resolves).

       2e. RENDER CARD STATES. In `ConnectionCard`, compute `is_connecting = connecting.read().as_deref() == Some(conn.name.as_str())`
           and `card_err = connect_error.read().as_ref().filter(|(n,_,_)| *n == conn.name).cloned()`.
           While `is_connecting`, show "Connecting…"; when `card_err` is Some, render the inline
           message + a Retry button gated on the retriable bool (reuse `AsyncView` or inline markup
           mirroring it). Retry re-runs the same connect closure (a fresh `on_connect.call((conn.clone(), auth))`
           with a freshly-built AuthInput) — NOT Resource::restart() (this is a spawn one-shot).
           Keep the offline card disabled (status Offline → not connectable) and stable list keys
           (`key: "{conn.name}"`).

    3. Disconnect release (CONN-07): wherever `active.set(None)` happens on disconnect, ALSO call
       `real.disconnect()` to drop the live client:
       - routes.rs Cmd-D handler (`Key::Character(c) if meta && c == "d"`) — it sets `active.set(None)`;
         add a `use_context::<Rc<NodeDbConnectionService>>()` and call `.disconnect()` there.
       - connection_popover.rs "Disconnect" action (`onclick: move |_| active.set(None)`) — same.
       - command_palette.rs "Disconnect" item (`active.set(None)`) — same.
       Ensure no long-lived closure clones the `NativeClient` itself (only the `Rc<service>`), so the
       client is genuinely released (Pitfall 6).

    4. Quick-switch (D-08) — connection_popover.rs and command_palette.rs currently call
       `service.connect(name)` (mock by-name). Reuse the REAL path: on switch, first
       `real.disconnect()` (drop current), then look up the target `SavedConnection` in the registry
       (`registry.read().iter().find(|c| c.name == target).cloned()`). For trust-mode targets, build
       `AuthInput::Trust` and run the same `connect_real` flow directly. For
       password/api_key/oidc targets — which need a secret NOT held in session (D-01 keeps no secret
       around) — route the user to the connect form/prompt rather than connecting with an empty
       secret or failing silently (D-08): set the active route/modal back to the connection manager
       (or open `ModalKind::NewConnection` pre-filled for that connection) so the secret can be
       re-entered on a card. Keep the popover/palette closing behavior.

    Constraints throughout: async only at the seam via `spawn`; never hold a signal guard across
    `.await`; `e.prevent_default()` on any form submit; no `_ =>` on AuthMode; no `.unwrap()` in
    non-test code; the in-session Secret is created at the call site (from the card-local
    `secret_input` signal) and moved into connect_real — never stored in SavedConnection or a
    long-lived signal.
  </action>
  <verify>
    <automated>cargo build -p nodedb-studio && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo nextest run -p nodedb-studio</automated>
  </verify>
  <acceptance_criteria>
    - `nodedb-studio/src/views/connection_manager.rs` contains `connect_real` and `is_retriable` (verify: `grep -n 'connect_real\|is_retriable' nodedb-studio/src/views/connection_manager.rs` matches both)
    - `ConnectionCard`'s `on_connect` prop carries the full SavedConnection + AuthInput, not a bare name (verify: `grep -n 'EventHandler<(SavedConnection, AuthInput)>' nodedb-studio/src/views/connection_manager.rs` matches), and the card calls `on_connect.call((conn.clone()` (verify: `grep -n 'on_connect.call((conn.clone()' nodedb-studio/src/views/connection_manager.rs` matches) — proving the handler receives the full SavedConnection, not just `name`
    - The card-local in-session secret is sourced from a `use_signal` whose value feeds `Secret::new` (verify: `grep -n 'secret_input' nodedb-studio/src/views/connection_manager.rs` matches AND `grep -n 'Secret::new' nodedb-studio/src/views/connection_manager.rs` matches), and `secret_input` is read with `.peek()` in the handler (verify: `grep -n 'secret_input.peek()' nodedb-studio/src/views/connection_manager.rs` matches)
    - The on_connect handler does NOT set `active` on the Err branch (verify by reading: `active.set(Some(...))` appears only inside the Ok branch)
    - `nodedb-studio/src/app.rs` provides `NodeDbConnectionService` via context and no longer has a throwaway `_real` binding (verify: `grep -n 'NodeDbConnectionService' nodedb-studio/src/app.rs` matches; `grep -n '_real' nodedb-studio/src/app.rs` returns nothing)
    - `disconnect()` is called in routes.rs, connection_popover.rs, and command_palette.rs (verify: `grep -rn '\.disconnect()' nodedb-studio/src/components/popovers/connection_popover.rs nodedb-studio/src/components/command_palette.rs nodedb-studio/src/routes.rs` matches all three)
    - No `use_resource` is used for the connect action (verify: connect/switch handlers use `spawn`)
    - `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes
    - `cargo nextest run -p nodedb-studio` exits 0
  </acceptance_criteria>
  <done>
    Clicking a connectable card collects the in-session secret from a card-local signal, builds the per-mode AuthInput, hands the full SavedConnection + AuthInput to the manager via `EventHandler<(SavedConnection, AuthInput)>`, runs connect_real at the seam, shows Connecting then inline error+Retry (gated on is_retriable) on failure without entering the connected state, and transitions to Studio on success; Cmd-D and both quick-switch surfaces release the live client via disconnect(); quick-switch reuses connect_real (and routes to the form when the target's secret isn't held — D-08); build + clippy + nextest are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Render the real identity in the topbar chip (CONN-06)</name>
  <files>nodedb-studio/src/components/topbar.rs</files>
  <read_first>
    - nodedb-studio/src/components/topbar.rs (current chip renders ONLY c.name, c.sub, c.current_database, c.avatar_letter(), c.capabilities.readonly — it does NOT render c.user or c.role today)
    - nodedb-studio/src/state/connection.rs (ActiveConnection: user, role, current_database, databases, sub; avatar_letter())
    - .planning/phases/02-connect-auth-capabilities/02-CONTEXT.md § Specific Ideas (chip shows user · role · current_database · N dbs · server_version)
    - .planning/phases/02-connect-auth-capabilities/02-RESEARCH.md § Target 2 (ActiveConnection population table — sub = "nodedb · {version}" / "{n} dbs")
  </read_first>
  <action>
    Surface the real identity in the connection chip. The `ActiveConnection` is now populated from
    the live probe (Plan 02 `describe_connection`), so the chip must show the real values rather than
    relying on placeholder text. NOTE: `c.user` and `c.role` are NET-NEW chip elements — the current
    topbar renders neither; you are ADDING them, not confirming an existing binding.

    - The connection chip already shows `{c.name}` and `{c.sub}` (keep `c.sub` — composed in Plan 02
      as `"nodedb · {server_version} · {n} dbs"` — rendering as the engine/sub line; do not change
      that format string, it must stay identical to Plan 02's `describe_connection` output).
    - ADD a `{c.user}` element to the chip (e.g. a `span { class: "user", "{c.user}" }` near the name)
      — this element does not exist today.
    - ADD a CONDITIONAL `{c.role}` element: render a `span { class: "role", "{c.role}" }` ONLY when
      `!c.role.is_empty()` (role may be `""` for api_key/oidc fallback — render nothing, and no empty
      separator, when role is empty). This element does not exist today.
    - The avatar already uses `c.avatar_letter()` (derived from the real user) — keep it.
    - The database chip already shows `{c.current_database}` — keep it bound to the live value.
    - The read-only pill already gates on `c.capabilities.readonly` — keep it (readonly leans false
      this phase per D-05; the pill simply won't show, which is correct).
    - Do NOT introduce any hardcoded identity strings; everything comes from `c` (the live
      ActiveConnection). No new mock values.

    Keep the existing popover toggle handlers and `stop_propagation` intact. No async here (read-only
    render of the live session signal).
  </action>
  <verify>
    <automated>cargo build -p nodedb-studio && cargo clippy -p nodedb-studio --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `nodedb-studio/src/components/topbar.rs` references both `c.user` and `c.role` (verify: `grep -n 'c\.user' nodedb-studio/src/components/topbar.rs` matches AND `grep -n 'c\.role' nodedb-studio/src/components/topbar.rs` matches)
    - The role element is conditional on a non-empty role (verify: `grep -n 'c.role.is_empty()' nodedb-studio/src/components/topbar.rs` matches)
    - The chip still binds `c.sub` and `c.current_database` (verify: `grep -nE 'c\.sub|current_database' nodedb-studio/src/components/topbar.rs` matches)
    - No hardcoded username/role/version literals were added (verify by reading: all chip text comes from `c.*`)
    - `cargo build -p nodedb-studio` succeeds and clippy is clean
  </acceptance_criteria>
  <done>
    The topbar chip renders the live session's user (net-new element), role when present (net-new conditional element), current database, sub line (server version + db count), and avatar letter — all from the real ActiveConnection, with the read-only pill gated on the live capability.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify the end-to-end connect/auth/capability/identity/disconnect flow against a live :6433</name>
  <what-built>
    The full real-connect path: form (D-02), inline connect card with Connecting/error/Retry (D-03),
    connect_real at the seam building the client per auth mode (CONN-01/02), forcing a probe to
    populate real capabilities (CONN-05) + identity (CONN-06), transitioning to the Studio shell
    (CONN-04), and releasing the client on Cmd-D (CONN-07). Quick-switch reuses the real path (D-08).
    Unit tests already cover client build, OIDC PoolConfig, capability mapping, identity parse, error
    mapping, and disconnect — this checkpoint confirms the LIVE round-trip that has no automated test
    (no in-process server harness; see 02-VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    Prerequisite: a NodeDB instance reachable on `:6433`. If none is available, note that and treat
    this as a deferred manual gate (the unit-tested paths still stand).

    1. Run: `cargo run -p nodedb-studio`
    2. SUCCESS (CONN-01/02/04/05/06): connect with a valid mode (e.g. password) to the live `:6433`.
       Expect: the card shows "Connecting…" briefly, then the app enters the Studio shell. Confirm:
       - The topbar chip shows a real username, role (if the server returns one), current database,
         "N dbs", and a non-empty server version in the sub line.
       - The rail / admin sub-tabs / capability-gated views reflect the server's real capabilities
         (e.g. Graph/FTS/Streams tabs appear only if the server advertises those bits) — not all-on
         mock flags.
    3. BAD CREDENTIALS (CONN-03 Auth): connect with a wrong password. Expect: an inline error on the
       card, NO Retry button (Auth is not retriable), and the app STAYS on the connection manager
       (never enters Studio).
    4. REFUSED/UNREACHABLE (CONN-03 Connection): connect to a wrong host/port (e.g. `:6499`). Expect:
       an inline error WITH a Retry button (Connection is retriable), app stays disconnected; clicking
       Retry re-runs the connect.
    5. DISCONNECT (CONN-07): from the connected shell press ⌘D. Expect: returns to the connection
       manager. (Optional: confirm sockets close — e.g. `lsof -p <pid> | grep 6433` shows no lingering
       connection after disconnect.)
    6. QUICK-SWITCH (D-08): from the connected shell, use the connection popover or ⌘K palette to
       switch to another saved connection. Expect: it disconnects the current and connects the target
       via the real path (prompting for a secret if the target needs one).
  </how-to-verify>
  <action>
    This is a human-verify checkpoint, not autonomous work. The executor pauses here. Follow the
    <how-to-verify> steps against a live :6433 instance (or record that no instance is available and
    defer this gate). Do not mark the phase done until the user types the resume signal.
  </action>
  <files>nodedb-studio/src/views/connection_manager.rs, nodedb-studio/src/app.rs, nodedb-studio/src/components/topbar.rs</files>
  <verify>
    <automated>cargo run -p nodedb-studio  # manual: exercise the flows in how-to-verify; no automated assertion (live-server gate)</automated>
  </verify>
  <done>User has run the live flows (or explicitly deferred for lack of a :6433 server) and typed the resume signal.</done>
  <resume-signal>Type "approved" if all flows behave as described, or describe the issues (e.g. "no caps tabs after connect", "Retry shown on bad password", "chip blank") to drive gap closure.</resume-signal>
</task>

</tasks>

<verification>
- `cargo build -p nodedb-studio` succeeds; `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes; `cargo nextest run -p nodedb-studio` is green.
- Form defaults to 6433 with the D-02 dropdown; no mTLS/keychain remnants.
- Connect failure never sets `active`; Retry is gated on is_retriable.
- Disconnect calls `NodeDbConnectionService::disconnect()` from all three disconnect surfaces.
- Manual e2e checkpoint confirms the live round-trip (or is explicitly deferred if no :6433).
</verification>

<success_criteria>
- CONN-01/02: user opens a real session with each auth mode from the manager (form + connect_real).
- CONN-03: failures show inline error (Retry only when retriable); app never enters connected state.
- CONN-04: success transitions to the Studio shell bound to the live session.
- CONN-06: the topbar chip shows the real user/role/db/dbs/version.
- CONN-07: ⌘D returns to the manager and releases the client.
</success_criteria>

<output>
After completion, create `.planning/phases/02-connect-auth-capabilities/02-03-SUMMARY.md`
</output>
</content>
</invoke>
